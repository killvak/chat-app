# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'tesss' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for tesss
 	pod 'SwiftyJSON'
 pod 'Alamofire'
 pod 'Fuzi', '~> 1.0.0'
  
  target 'tesssTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'tesssUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
